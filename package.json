{
  "name": "sos-prescription-v3",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm --prefix medlab-worker run start"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "puppeteer-core": "^23.4.1",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "medlab-worker"
  ],
  "engines": {
    "node": "24.x"
  }
}
